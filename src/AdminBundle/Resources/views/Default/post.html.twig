{% extends 'container_admin.html.twig' %}

{% form_theme form  'form/fields.html.twig'  %}

{% block title %}Gestion {{app.user.site.name}} - {{sectionSite.section.title}}{% endblock %}

{% block content %}

    <h3>{{sectionSite.section.title}}</h3>
    
        
        {{form_start(form)}}
        <div class="form-group row">
            <div class="col-sm-12">
              {{form_widget(form.content, {"attr":{"class":"form-control", 'placeholder':"Description"}})}}
            </div>
          </div>
        {{form_rest(form)}}
        <button type="submit" class="btn btn-primary btn-success">Enregistrer</button>
        {{form_end(form)}}
  
   

{% endblock %}
