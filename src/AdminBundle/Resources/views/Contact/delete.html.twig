{% extends 'container_admin.html.twig' %}

{% block title %}Gestion {{app.user.site.name}} - Contact{% endblock %}

{% block content %}

    
    <h3>Etes vous sûr de vouloir supprimer le contact {{contact.name}} ?</h3>
    <div class="row">
        <div class="col-sm-12">
            {{form_start(form)}}
            {{form_rest(form)}}
            <button type="submit" class="btn btn-danger"><i class="far fa-trash-alt"></i> Oui</button>
            <a href="{{path('admin_contact_index')}}" class="btn btn-primary">Non</a>
            {{form_end(form)}}
        </div>
    </div>

{% endblock %}
